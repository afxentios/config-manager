[report]

exclude_lines =
    # Don't complain if tests don't hit defensive assertion code for package importing:
    except ImportError

    # Don't complain if tests don't hit KeyError validation:
    except KeyError